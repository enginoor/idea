# agent.md

This file is the working memory for this product. It exists so that any future session starts from the thinking that shaped this app instead of rediscovering it. Read it before you change code. If a change fights something written here, update this file first and say why in your response.

## What this product is

Name: OriginCheck. A native macOS app that answers one question: was this text or file generated by Claude?

It works through two technically opposite signals:

1. Text watermark analysis. Claude models launched on or after August 2, 2026 embed an invisible statistical watermark in generated prose. It travels with copy and paste, it survives light editing, and short or heavily edited passages carry no reliable signal. Anthropic has announced a detection API but has not released it, so the text side is a pluggable provider interface that reports the honest unavailable state until then.
2. C2PA metadata verification. Claude attaches signed provenance metadata to generated files such as png, jpg, and svg. This side works today, is cryptographically verifiable, and is the product's core credibility.

The repo is enginoor/idea. The owner's spec is complete and detailed; this file distills the thinking behind it so future sessions do not need to reread the whole document.

## The core asymmetry

A positive signal exists; a negative signal does not. A valid Claude-signed manifest is strong evidence the file was processed by Claude. The absence of a manifest proves nothing, because any re-save, conversion, screenshot, or upload pipeline can strip it. The same is true for the watermark: no mark can mean edited, paraphrased, translated, short, or generated by an older model.

The whole product follows from that: every verdict carries a caveat, every verdict carries evidence, and no negative result is ever presented as proof of human authorship.

## Product principles

1. Be the honest detector. Confidence is a range, not a verdict. Every screen educates, never accuses.
2. Positives are claims, not judgments. A detected mark means Claude likely processed the content. It does not prove AI authorship, plagiarism, or dishonesty.
3. Unknowns reduce confidence, never increase it. Unknown signer, expired certificate, missing metadata, short text: each pushes toward inconclusive.
4. The engine is the product. It is a pure Swift library with no UI imports, unit tested in CI, and treated like a library even inside the app.
5. Local and private by default. No telemetry, no accounts, no cloud. Raw content is stored only with explicit per-record consent; by default only a SHA-256 hash is kept.
6. No circumvention. No watermark removal, no detection-dodging advice. Ever.
7. Use the reference tooling. C2PA verification is done by c2patool, not reimplemented. The product's value is the UX and honest framing around verification, not the verification itself.
8. No overclaiming in marketing. The file side is real today; the text side is advertised as coming.

## Voice and punctuation rules, hard rules

- No em dashes anywhere: UI copy, code comments, this file, chat replies. Use commas, periods, colons, or a new sentence.
- No emojis anywhere in the product.
- No decorative icons. An icon may earn a place only if a word would be worse.
- Banned words and phrases: unleash, supercharge, seamless, elevate, empower, journey, unlock, dive into, game changer, level up, AI-powered, state of the art, robust, cutting-edge, revolutionize, effortless, magic.
- Write short sentences. Prefer concrete nouns. Say a verdict, not a decision framework.
- The caveat strings live in `Caveats.swift` and are part of the product contract. Never weaken them.

## Architecture

The engine (`Sources/OriginCheckEngine`) has no SwiftUI imports. Layers depend downward only:

- Models: `VerdictKind`, `Confidence`, `EvidenceItem`, `TextVerdict`, `FileVerdict`, `C2PAClaim`, `HistoryRecord`, `AnalysisOptions`, `ThresholdPreset`.
- `C2PAVerifier`: runs `c2patool` (path injectable), parses the manifest JSON leniently, maps to a `FileVerdict`. Unparseable output is reported as no manifest, never as a verdict on authorship.
- `MediaFormat`: the list of file formats c2patool can verify today, with case-insensitive extension parsing and display names. Covers images (png, jpg, jpeg, svg, webp, avif, heic, heif, tif, tiff, dng), video (mp4, mov), audio (m4a, mp3, wav), and pdf (read-only).
- `FolderVerifier`: scans a folder recursively, verifies every supported file with `C2PAVerifier`, and returns a `BatchReport` (summary counts, per-file verdicts, failures). One broken file does not stop the scan; hidden files and unsupported extensions are skipped. `toolMissing` flags the case where c2patool cannot be launched.
- `TextWatermarkAnalyzer`: `TextWatermarkProvider` protocol, `LocalStatisticalAnalyzer` (honest unavailable), `AnthropicDetectionAPIProvider` (scaffolded, feature flagged, Keychain key via `KeyStoring`).
- `VerdictCombiner`: merges provider results. A positive signal beats a negative one and the conflict is shown; short text caps confidence; all-unavailable reports notAvailable.
- `HistoryStore`: `HistoryStoring` protocol, `JSONHistoryStore` actor, `HistoryRecorder` with hash-only defaults.
- `SHA256`: dependency-free, tested against known vectors.

The app (`App/`) is a macOS-only SwiftUI package: Check tab (paste text or drop a file), History tab (search, export JSON, delete), Settings tab (providers, thresholds, retention, Keychain key), and a menu bar extra with clipboard check.

## Current state

Built 2026-08-15, fifth session with code. The first session's web app was removed on the owner's request; this product replaced it.

- Fourth bug-hunt pass, 2026-08-15. The batch banner promised "set the tool path in Settings" but no such setting existed; Settings now has a real c2patool path field, persisted under the `c2paToolPath` defaults key, and AppState rebuilds the engine when it changes. File hashing for history records ran synchronously on the main actor, so verifying a large video froze the UI while the stream was read; the record is now built in a detached task and only the small JSON write runs on the main actor. A history-save failure after a successful verdict was reported as "Analysis failed" and clobbered the verdict; it now says the verdict is shown but could not be saved. The batch report card had no scroll container and an uncapped per-file list, so a large folder overflowed the window; it now scrolls and caps per-file rows at 100 with a +N more line. Folder scans were strictly sequential (one tool run at a time, 30 s each); they now verify in bounded parallel chunks of 8 while staying deterministic (outcomes are sorted afterwards). Verifying a nonexistent path used to return a silently empty report card; `FolderVerifier` now throws `FolderVerifierError` (directoryNotFound / directoryUnreadable) instead. The drop zone ran c2patool on dropped folders and produced a bogus no-manifest verdict; folders now route to the folder scan, and drops that were skipped by a running check or contained no file URLs say so instead of staying silent. Engine suite: 49 tests pass on Linux Swift 6.0.3 (2 new: missing directory throws, file path instead of directory throws).
- Third bug-hunt pass, 2026-08-15. SHA-256 hashing was quadratic: `Digester.update` appended each chunk to a buffer and shifted it one element at a time, so 8 MiB took 6.5 s and a large video was effectively unhashable. It now compresses full 64-byte blocks straight from the input with a small carry buffer; 8 MiB hashes in about 0.6 s in debug mode, and a regression test locks in the linear bound. `modificationState` no longer reports "modified after signing" when the signature does not verify: a hash mismatch only proves modification if the manifest can be attributed to its signer, so an expired certificate plus a hash mismatch stays inconclusive with no modification claim (new fixture `expired-hash-mismatch.json`). Insufficient-input verdicts now carry their own caveat (`Caveats.textTooShort`) instead of the "no watermark detected" text. App fixes: the Cmd+Shift+C and Cmd+Shift+O shortcuts declared on menu bar items never fired, because a menu bar menu is only active while it is open; they moved to a real Verify menu in the app scene, and the menu bar items no longer show misleading shortcut hints. The file picker activates the app first so the panel cannot open behind the frontmost app. Settings no longer goes stale after saving or removing the API key: key presence is observable state instead of a Keychain read, and save failures surface a message instead of being swallowed by `try?`. A multi-file drop now says how many files were checked, because the panel only keeps the last verdict. Engine suite: 47 tests pass on Linux Swift 6.0.3.
- Second bug-hunt pass, 2026-08-15. The previous pass shipped a duplicate `storeRawContent` declaration in AppState (the settings block re-declared a property that already existed above it), which turned CI red with `invalid redeclaration`. Removed the duplicate; the macOS app target compiles again on push.
- c2patool runs now have a timeout (30 s default, injectable). A hung tool previously left `isAnalyzing` true forever and blocked every later check. The verifier terminates the process, then escalates to SIGKILL when Foundation's SIGTERM delivery fails (observed on Linux: `terminate()` left the busy child alive and unreaped), then fails the check with an honest message. Batches treat a timeout as a per-file failure, not a missing tool.
- History corruption is quarantined, not fatal. A history.json that fails to decode is moved aside as `history-corrupt-<timestamp>.json` and the store starts fresh, so one corrupt byte can no longer brick every add and delete.
- Batch report UI no longer floods: when the tool is missing the banner alone explains it (the per-file list would repeat the same cause N times), and a real failures list is capped at 20 rows with a +N more line.
- Drop zone now verifies every dropped file in order; it used to silently check only the first. The menu bar opens the main window before a clipboard or file check runs, so the user sees progress. History lists newest first.
- Engine suite is 47 tests, all passing on Linux Swift 6.0.3. The app edits are compiled by CI on push, since the sandbox cannot build SwiftUI.

- Full engine implemented and verified: 38 unit tests pass on Linux Swift 6.0.3 (C2PA verdict mapping, confidence rules, combiner behavior, history persistence, SHA-256 vectors, media format parsing, batch folder scans).
- Batch folder verification shipped at the engine level: `FolderVerifier` walks a folder, verifies each supported file, and returns a report card with counts, per-file verdicts, and failures. Tested against all supported format families through the mock tool.
- Format support expanded to the full c2patool list: webp, avif, heic, heif, tif, tiff, dng, mp4, mov, m4a, mp3, wav, and pdf join the original png, jpg, jpeg, svg. The macOS app's file importer and drop zone now accept the full list too, derived from the engine's MediaFormat table so the UI can never drift from what verification supports. Folder scans already accepted everything.
- Fixtures: Claude-signed intact, modified after signing, unknown signer, expired certificate, plus a mock c2patool for demos.
- The SwiftUI app compiles and is verified: CI builds it on macOS runners on every push. The Linux sandbox cannot compile it, so CI is the compile gate. The first real build surfaced Swift 6 concurrency errors that are now fixed: AppState is @MainActor, every Task that touches it is explicitly @MainActor, and the text editor helper binds correctly.
- Settings are stored observable properties on AppState, loaded from UserDefaults in init and persisted by one onChange hook in the app scene. Do not convert them back to computed properties over UserDefaults: the Observation framework only tracks stored state, so computed settings made the Settings UI blind to changes (the API key field never appeared) and silently broke the raw-content preference persistence.
- The engine was compiled and tested in the sandbox with a downloaded Swift toolchain.

## Known tradeoffs and open items

- The text detection API does not exist yet. The honest unavailable state is the product until Anthropic ships it.
- File verification depends on c2patool being installed. Ship instructions and consider vendoring it.
- History is JSON, not SQLite. Acceptable for the MVP; swap to GRDB or SwiftData when volume demands it.
- The app target is compiled on macOS by GitHub Actions CI on every push, and by the release workflow in release mode. The development sandbox is Linux, so CI is the real compile gate. The app is ad-hoc signed by the packaging script but not yet sandboxed or notarized; both need a developer certificate or the owner's Mac.
- The manifest parser uses heuristic mapping for signature validity and modification state. It is tested against the fixtures and documented; real-world c2patool output should be fed back into the fixture library as it appears.
- Modification is only reported when the signature verifies. A hash mismatch with an invalid or unverifiable signature proves nothing about the signer, so the verdict stays inconclusive instead of claiming the file changed after signing.

## Roadmap, in priority order

1. Build the app on a Mac, fix whatever Xcode surfaces, sandbox and notarize. Done through CI: the app compiles on macOS runners on every push and in the release pipeline. Sandboxing and notarization remain open and need an Apple Developer certificate.
2. Real Anthropic detection API integration the moment it ships, behind the existing provider interface.
3. Expand file formats: done at the engine level (full c2patool list) and in the app. The picker and drop zone derive their allowed types from MediaFormat, so a future format addition flows to the UI automatically.
4. Batch folder verification: engine done and tested. Remaining work is Mac-side only: build the report card UI and decide whether batches write to history (currently they do not; a batch has no single verdict kind).
5. SQLite-backed history.
6. Multi-model support later; the provider architecture already isolates model-specific logic.

## Working notes for future sessions

- Engine tests: `swift test` (works on Linux and macOS). The sandbox has Swift at /opt/swift/usr/bin; add it to PATH if it is not there.
- CI/CD: workflows live in `.github/workflows/`. `ci.yml` tests on every push and pull request and can be run manually via workflow_dispatch; `release.yml` builds and publishes on `v*` tags and via workflow_dispatch. Packaging and notes are `Scripts/package-app.sh` and `Scripts/release-notes.sh`. Each release carries the zip plus a machine-readable checksums.txt (verified in the workflow) and the SHA-256 in the notes. Releases are ad-hoc signed, not notarized.
- Release process verified end to end: v0.1.0 is published with a zipped OriginCheck.app, a SHA-256 checksum in the notes, and the structured template. Future releases: tag vX.Y.Z and push; the pipeline gates on tests, builds, packages, and publishes.
- Settings include the c2patool path under `c2paToolPath` (defaults key `c2paToolPath`, default `c2patool`). `AppState.setC2PAToolPath` rebuilds the engine; `verifyFolder` always reads the current path from the engine. Never prefill that field with anything but the current stored value.
- `FolderVerifier` verifies in parallel chunks of 8 and throws `FolderVerifierError.directoryNotFound` / `.directoryUnreadable` instead of returning an empty report for a bad path. The batch card scrolls and caps per-file rows at 100.
- Swift 6 gotcha in the app: `Task {}` does not inherit MainActor isolation, and observable app state must be `@MainActor` or every capture into a Task is a data-race error. Follow the existing patterns in CheckView and MenuBar when adding async UI work.
- The engine's `C2PAVerifier` runs c2patool in a detached task and drains stdout and stderr on background threads. Keep the tool run off the caller's executor (a blocking waitUntilExit on the main actor freezes the UI) and never read the pipes only after waitUntilExit (a manifest larger than the 64 KiB pipe buffer deadlocks the run).
- SwiftPM gotcha: the app package references the engine by path, and path dependencies are identified by the checkout folder name, not the manifest name. The repo folder is `idea`, so `App/Package.swift` uses `package: "idea"`. Renaming the checkout folder breaks the build; CI checks out into the repo name so it always matches.
- The platform's web checks do not apply to this repo anymore. The engine test suite is the verification gate.
- Keep copy in the tone above. Re-read any sentence that uses an em dash or a banned word.
- Standing rule: every change is committed and pushed to GitHub at the end of the turn. context.md logs each session.
- When in doubt, say less and mean it. This product is trusted precisely because it refuses to overclaim.
